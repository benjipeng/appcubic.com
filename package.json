{
  "name": "appcubic-landing",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "sync": "node scripts/sync-workshop.mjs",
    "build": "node scripts/sync-workshop.mjs && node scripts/discovery.mjs && astro build && node scripts/check-publication.mjs",
    "build:offline": "node scripts/discovery.mjs && astro build && node scripts/check-publication.mjs",
    "test": "node --test scripts/*.test.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@lucide/astro": "^1.23.0",
    "astro": "^7.0.5"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^1.0.0"
  }
}
